server:
  port: 80
spring:
  application:
    name: cloud-consumer-order
  cloud:
    #注册到consul地址
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}